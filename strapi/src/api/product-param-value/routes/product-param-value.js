'use strict';

/**
 * product-param-value router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::product-param-value.product-param-value');
