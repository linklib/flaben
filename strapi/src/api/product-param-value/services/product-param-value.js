'use strict';

/**
 * product-param-value service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::product-param-value.product-param-value');
